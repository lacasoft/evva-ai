{
  "name": "evva",
  "version": "0.1.0",
  "private": true,
  "description": "Evva — Tu asistente personal con memoria real y acción proactiva",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:gateway": "pnpm --filter @evva/gateway dev",
    "dev:worker": "pnpm --filter @evva/worker dev",
    "dev": "concurrently \"pnpm dev:gateway\" \"pnpm dev:worker\"",
    "build": "pnpm --filter @evva/core build && pnpm --filter @evva/database build && pnpm --filter @evva/ai build && pnpm --filter @evva/skills build && pnpm --filter @evva/gateway build && pnpm --filter @evva/worker build",
    "build:gateway": "pnpm --filter @evva/gateway build",
    "build:worker": "pnpm --filter @evva/worker build",
    "test": "pnpm --recursive test",
    "lint": "eslint \"packages/**/*.ts\" \"apps/**/*.ts\"",
    "lint:fix": "eslint \"packages/**/*.ts\" \"apps/**/*.ts\" --fix",
    "format": "prettier --write \"packages/**/*.ts\" \"apps/**/*.ts\"",
    "format:check": "prettier --check \"packages/**/*.ts\" \"apps/**/*.ts\"",
    "typecheck": "pnpm --recursive typecheck",
    "db:migrate": "pnpm --filter @evva/database migrate"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "eslint": "^10.2.0",
    "prettier": "^3.8.1",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.58.0",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
